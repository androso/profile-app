import { useSignIn } from '@clerk/clerk-expo';
import { Link } from 'expo-router';
import { useState } from 'react';
import { Pressable, Text, TextInput, View } from 'react-native';
export default function SignIn() { const { signIn, setActive, isLoaded } = useSignIn(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const submit=async()=>{if(!isLoaded)return;try{const result=await signIn.create({identifier:email,password});await setActive({session:result.createdSessionId});}catch(e){setError(e instanceof Error?e.message:'Unable to sign in');}}; return <View className="flex-1 justify-center gap-4 bg-slate-950 px-6"><Text className="text-3xl font-bold text-white">Sign in</Text><TextInput className="rounded-xl bg-slate-800 p-4 text-white" placeholder="Email" placeholderTextColor="#94a3b8" autoCapitalize="none" value={email} onChangeText={setEmail}/><TextInput className="rounded-xl bg-slate-800 p-4 text-white" placeholder="Password" placeholderTextColor="#94a3b8" secureTextEntry value={password} onChangeText={setPassword}/><Pressable className="rounded-xl bg-indigo-500 p-4" onPress={submit}><Text className="text-center font-semibold text-white">Continue</Text></Pressable>{error?<Text className="text-red-400">{error}</Text>:null}<Link href="/sign-up" className="text-center text-indigo-300">Create an account</Link></View>; }
